import Container from "@/components/container";
import React from "react";

const LogoSection = () => {
  return (
    <Container>
      <div className="min-h-screen flex items-center justify-center">LogoSection</div>
    </Container>
  );
};

export default LogoSection;
